Identifiers – case sensitive, letters, letter numbers, _    Program defined (Args, coolFunVar, etc)    Other programmer defined (system, println)    Not part of the proper language    Reserved words (public, class, void)Variables – location in memory that is used to hold a data value    // double pi = 3.14159;    Java is a string-Typed Language. The following will error because of type definitions        int total;        total="xyz";    Constants:        final int months = 12;    Datatypes: [number of bytes]        integers (byte[8], short[16], long[32], int[64])        float points (float[32], double[64])        Literal value (eg. 12)            integer - int                To force long, use "12L"            floating point - double                To force float, use "12.3f"        Character primitive            char someChar = 'q';            Java uses UNICODE                2^16 bits of memory instead of 2^8 with ASCCI        Boolean            true or falseExpressions - combination of one or more operators and operands    + - * / %    Order of operations